SLACK_WEBHOOK_URL=https://hooks.slack.com/services/your/webhook/url
INCLUDE_IMMEDIATE_SUPPLY=true
DB_PATH=data/alerts.db

# Optional: true threaded replies in Slack require Web API credentials.
# Without these, the bot sends only the parent Block Kit summary via webhook.
SLACK_BOT_TOKEN=
SLACK_CHANNEL_ID=
